// import { test } from '@playwright/test';

// // Run only in Chromium (Chrome)

    

//   test('Test in Chrome', async ({ page }) => {
//   test.skip(browserName !== 'chromium', 'Only runs in Chrome');
  
//     await page.goto('https://example.com');
//     await page.pause()
//   });


// // Run only in WebKit (Safari engine)

     

//   test('Test in WebKit', async ({ page }) => {
//       test.skip(browserName !== 'webkit', 'Only runs in WebKit');

 

//     await page.goto('https://example.com');
//     await page.pause()
//   });
